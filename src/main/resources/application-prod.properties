# Production Database Configuration (uses Render's DATABASE_URL)
spring.datasource.url=${DATABASE_URL}

# JPA and Flyway for production
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true

# JWT Secret Configuration (uses Render's JWT_SECRET)
jwt.secret=${JWT_SECRET}